<%= form_for(@location_group) do |f| %>
  <% if @location_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_group.errors.count, "error") %> prohibited this location_group from being saved:</h2>

      <ul>
      <% @location_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
